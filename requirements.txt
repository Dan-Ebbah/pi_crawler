# Core dependencies
requests>=2.28.0
beautifulsoup4>=4.11.0
typer>=0.9.0

# Database (optional - for PostgreSQL storage)
psycopg2-binary>=2.9.0

# API (optional - for FastAPI endpoint)
fastapi>=0.100.0
uvicorn>=0.22.0

# LLM enrichment (optional - install separately)
# anthropic>=0.18.0
